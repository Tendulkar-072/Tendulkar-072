#include <stdio.h>

int sum(int, int);

int sum(int x ,int y) {
    int sum;
    sum = x + y;
    return sum;
}

int main()
{
    int a, b;
    
    printf("Enter values of two numbers :\n");
    scanf("%d%d", &a, &b);
    
    printf("Sum of %d and %d is %d", a, b, sum(a, b));
    
}
